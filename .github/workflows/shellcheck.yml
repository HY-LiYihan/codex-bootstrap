name: script-checks

on:
  pull_request:
  push:
    branches: [main]

jobs:
  bash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Bash syntax check
        run: |
          bash -n install.sh
          bash -n agent
          find agents -name 'install.sh' -print0 | xargs -0 -I{} bash -n {}
          bash -n switch.sh
      - name: Codex dispatcher dry run
        run: AGENT=codex AGENT_TOKEN=test-token AGENT_BASE_URL=https://codex1.sssaicode.com/api/v1 ./install.sh --local . --dry-run --skip-codex-install --skip-shell-rc --yes
      - name: Codex safe profile dry run
        run: CODEX_SECURITY_PROFILE=safe AGENT=codex AGENT_TOKEN=test-token AGENT_BASE_URL=https://codex1.sssaicode.com/api/v1 ./install.sh --local . --dry-run --skip-codex-install --skip-shell-rc --yes
      - name: Default agent entrypoint dry run
        run: AGENT_TOKEN=test-token AGENT_BASE_URL=https://codex1.sssaicode.com/api/v1 AGENT_BOOTSTRAP_LOCAL_SOURCE=. AGENT_BOOTSTRAP_INSTALL_ARGS='--dry-run --skip-codex-install --skip-shell-rc --yes' bash -c '. ./agent'
      - name: Claude Code dispatcher dry run
        run: AGENT=claudecode AGENT_TOKEN=test-token AGENT_BASE_URL=https://node-hk.sssaicode.com/api ./install.sh --local . --dry-run --skip-claude-install
      - name: OpenClaw dispatcher dry run
        run: AGENT=openclaw AGENT_TOKEN=test-token AGENT_BASE_URL=https://node-hk.sssaicode.com/api ./install.sh --local . --dry-run
      - name: Codex++ dispatcher dry run
        run: AGENT=codexplusplus ./install.sh --local . --dry-run --skip-setup --provider-sync
      - name: Agent switch dry run
        run: |
          export AGENT_BOOTSTRAP_HOME="$(mktemp -d)"
          node switch.js add test --token test-token --base-url https://node-hk.sssaicode.com/api
          node switch.js list
          node switch.js use test --dry-run
          node switch.js shell-hook

  windows-powershell:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: PowerShell syntax check
        shell: pwsh
        run: |
          $errorsFound = $false
          Get-ChildItem -Recurse -Filter *.ps1 | ForEach-Object {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$tokens, [ref]$errors) | Out-Null
            if ($errors.Count -gt 0) {
              Write-Host "Syntax errors in $($_.FullName)"
              $errors | Format-List | Out-String | Write-Host
              $errorsFound = $true
            }
          }
          if ($errorsFound) { exit 1 }
      - name: Interactive entrypoint smoke check
        shell: pwsh
        run: |
          if (-not (Test-Path .\agent.ps1)) { exit 1 }
      - name: Codex PowerShell dry run
        shell: pwsh
        run: |
          $env:CODEX_TOKEN = 'test-token'
          $env:CODEX_API_URL = 'https://codex1.sssaicode.com/api/v1'
          $env:CODEX_SECURITY_PROFILE = 'safe'
          .\agents\codex\install.ps1 -LocalSource . -DryRun -SkipCodexInstall -SkipProfileUpdate
      - name: Claude Code PowerShell dry run
        shell: pwsh
        run: |
          $env:CLAUDE_CLIENT_TOKEN = 'test-token'
          $env:CLAUDE_API_URL = 'https://node-hk.sssaicode.com/api'
          .\agents\claudecode\install.ps1 -DryRun -SkipInstall
      - name: OpenClaw PowerShell dry run
        shell: pwsh
        run: |
          $env:OPENCLAW_TOKEN = 'test-token'
          $env:OPENCLAW_BASE_URL = 'https://node-hk.sssaicode.com/api'
          .\agents\openclaw\install.ps1 -DryRun
      - name: Codex++ PowerShell dry run
        shell: pwsh
        run: .\agents\codexplusplus\install.ps1 -DryRun -SkipSetup -ProviderSync 1
      - name: Agent switch PowerShell wrapper syntax
        shell: pwsh
        run: |
          $tokens = $null
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path .\switch.ps1).Path, [ref]$tokens, [ref]$errors) | Out-Null
          if ($errors.Count -gt 0) { $errors | Format-List | Out-String | Write-Host; exit 1 }
